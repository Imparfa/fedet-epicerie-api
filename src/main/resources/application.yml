spring:
  application:
    name: FEDET Epicerie Solidaire API
  datasource:
    url: 'jdbc:postgresql://localhost:5432/DistributionFEDET'
    username: admin
    password: secret
    driver-class-name: org.postgresql.Driver
  jpa:
    open-in-view: true
    generate-ddl: false
    show-sql: true
    hibernate:
      ddl-auto: update
  cloud:
    vault:
      uri: http://localhost:8200
      token: hvs.CAESILpMx-NePkdJKTUvUuSzDAtPEDpvSZHjTd4Z-YazCXwKGh4KHGh2cy5NR2J6V1Q1Z0g1YnZ3Q29WNEdiTjdMbG4
      authentication: TOKEN
      host: localhost
      port: 8200
      scheme: http
      kv:
        enabled: true
        backend: secret
        application-name: jwt-passphrase
#  web:
#    cors:
#      allowed-origin-patterns: http://localhost:8100
#      allowed-methods: GET,POST,PUT,DELETE,OPTIONS
#      allowed-headers: *
#      allow-credentials: true
#logging:
#  level:
#    org.springframework.security: DEBUG
#    org.springframework.web: DEBUG
#    root: DEBUG
---
spring.config.activate.on-profile: localhost
spring:
  datasource:
    url: "jdbc:h2:file:./target/db/embedded;MODE=POSTGRESQL;AUTO_SERVER=true"
    username: root
    password: root
    driver-class-name: org.h2.Driver
  jpa:
    hibernate:
      ddl-auto: create-drop

---
spring.config.activate.on-profile: test
spring:
  datasource:
    url: "jdbc:h2:file:./target/db/embedded;MODE=POSTGRESQL;AUTO_SERVER=true"
    username: root
    password: root
    driver-class-name: org.h2.Driver
  jpa:
    hibernate:
      ddl-auto: create-drop


---
spring.config.activate.on-profile: production
spring:
  datasource:
    url: 'jdbc:postgresql://localhost:5494/fedetEpicerieSolidaire'
    username: root
    password: root
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
